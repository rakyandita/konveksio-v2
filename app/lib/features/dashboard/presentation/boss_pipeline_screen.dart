import 'package:flutter/material.dart';

class BossPipelineScreen extends StatelessWidget {
  const BossPipelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Produksi')),
      body: const Center(
        child: Text('Halaman Pipeline/Kanban (Placeholder)'),
      ),
    );
  }
}
