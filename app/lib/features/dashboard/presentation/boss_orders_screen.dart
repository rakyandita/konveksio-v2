import 'package:flutter/material.dart';

class BossOrdersScreen extends StatelessWidget {
  const BossOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Order & Pesanan')),
      body: const Center(
        child: Text('Halaman Daftar Order (Placeholder)'),
      ),
    );
  }
}
